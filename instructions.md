Create a function called `getLarger`. This should take two number parameters and return the number which is larger. 

For example, calling getLarger with the numbers 0 and 10, we should return: 

```
10
```

